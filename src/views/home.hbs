<div class="container">
        <div class="sidebar">
            <h2 hx-get="/test" hx-swap="innerHTML">{{message}}</h2>   
            <div>
                <!--img src="path_to_profile_image.jpg" alt="Profile Image" width="50"-->
                <span>Alex Gomez</span>
                 <div id="sidebarMenu" class="sidebarMenu" hx-swap-oob="innerHTML" hx-target="#sidebarMenu">
                    {{#each sidebarMenuOptions}}              
                        <div class="MenuItem" hx-get="{{route}}" hx-target=".main-content" hx-swap="outerHTML">{{label}}</div>
                    {{/each}}
                </div>
            </div>
        </div>
        <div id="main-content" class="main-content">
            <div class="widget">
               Projects Widget
            </div>
            <div class="widget">
               Practices Widget
            </div>
            <div class="widget">
               Employees Widgets
            </div>
            <div class="widget">
               Other stuff and links to other tools?
            </div>
        </div>
    </div>
